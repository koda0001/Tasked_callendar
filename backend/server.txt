const express = require('express');
const mongoose = require('mongoose');
const cors = require('cors');
require('dotenv').config();
const eventsSchema = require('./models/events')

const app = express();

app.use(cors());
app.use(express.json());

const PORT = process.env.PORT || 5001;

mongoose.connect(process.env.MONGODB_URI, { useNewUrlParser: true, useUnifiedTopology: true })
.then(() => console.log('MongoDB Connected'))
.catch(err => console.log(err));



app.get('/getEvents', async (req, res) => {
  try {
    const db = await connectDB(); // Make sure to call the async function to connect to your db
    const collections = await client
    const events = await db.collection('events').find({}).toArray(); // Fetch all documents from the 'events' collection
    res.json(events); // Send the documents back to the client as JSON
    console.log("Connected correctly to server");
  } catch (error) {
    console.error("Failed to fetch events:", error);
    res.status(500).json({ message: "Failed to fetch events" });
  }
});

app.listen(PORT, () => console.log(`Server running on port ${PORT}`));
